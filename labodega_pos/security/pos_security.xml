<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
    Record rule to filter POS configs based on allowed users.
    If restrict_user_access is False OR allowed_user_ids is empty: show to all
    If restrict_user_access is True AND allowed_user_ids is set: show only to allowed users
    -->
    <record id="rule_pos_config_allowed_users" model="ir.rule">
        <field name="name">POS Config: Allowed Users Only</field>
        <field name="model_id" ref="point_of_sale.model_pos_config"/>
        <field name="groups" eval="[(4, ref('point_of_sale.group_pos_user'))]"/>
        <field name="domain_force">[
            '|',
            ('restrict_user_access', '=', False),
            '|',
            ('allowed_user_ids', '=', False),
            ('allowed_user_ids', 'in', user.id)
        ]</field>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>
</odoo>

