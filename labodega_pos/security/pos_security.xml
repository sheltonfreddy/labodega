<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
    Record rule to filter POS configs based on allowed users.
    Shows POS config if:
    - restrict_user_access is False, OR
    - allowed_user_ids is empty, OR
    - current user is in allowed_user_ids, OR
    - current user has an active session on this config (for closing sessions)

    This rule is global (no groups) so it applies to ALL users and combines
    with the existing multi-company rule using AND logic.
    -->
    <record id="rule_pos_config_allowed_users" model="ir.rule">
        <field name="name">POS Config: Allowed Users Only</field>
        <field name="model_id" ref="point_of_sale.model_pos_config"/>
        <field name="domain_force">[
            '|', '|', '|',
            ('restrict_user_access', '=', False),
            ('allowed_user_ids', '=', False),
            ('allowed_user_ids', 'in', user.id),
            ('session_ids.user_id', '=', user.id)
        ]</field>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>
</odoo>

